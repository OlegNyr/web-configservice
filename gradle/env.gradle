ext {
    registry = {}
    nexus = {}
    nexus.url =  System.getenv('CI_SERVER_URL')
    registry.user = System.getenv('CI_REGISTRY_USER')
    registry.password = System.getenv('CI_REGISTRY_PASSWORD')
    registry.url = System.getenv('CI_REGISTRY') ?: 'reg.agria.pro:4567'
    registry.image = System.getenv('CI_REGISTRY_IMAGE') ?: 'reg.agria.pro:4567/agri/proc'
    registry.baseImage = "${registry.url}/docker/jdk13@sha256:d0bb0df4e4936720414f50e5e801db706f4cd1fb0a0a348defaf45e865f579ef";
    registry.tag = "jib"
}